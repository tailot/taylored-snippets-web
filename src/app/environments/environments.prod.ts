/**
 * @fileoverview Configuration settings for the production environment.
 */

/**
 * Environment-specific configuration variables for production.
 */
export const environment = {
  /**
   * Flag indicating that the application is running in production mode.
   */
  production: true,
  /**
   * The orchestrator URL. Undefined in this configuration, potentially set elsewhere or not used in production.
   * @type {string | undefined}
   */
  orchestrator: undefined
};